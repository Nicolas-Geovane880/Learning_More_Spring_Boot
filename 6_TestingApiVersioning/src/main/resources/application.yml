spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testingApiVersioning
    username: sa
    password:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update

  server:
    error:
      include-stacktrace: on-param

logging:
  level:
    root: WARN

